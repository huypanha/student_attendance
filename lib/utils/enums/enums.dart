// ignore_for_file: constant_identifier_names

enum ASIconType {
  regular,
  solid,
  duotone,
}

enum ProgressType {
  upload,
  download,
}

enum CalendarRepeatType {
  none,
  daily,
  weekly,
}

enum UserType {
  teacher,
  student
}

enum ReturnPageType {
  edited,
  deleted
}